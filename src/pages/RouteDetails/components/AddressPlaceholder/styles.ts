import { SxProps, Theme } from '@mui/material/styles';

export const addressPlaceholderStyles: SxProps<Theme> = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  height: '100vh',
  width: '100%',
};
